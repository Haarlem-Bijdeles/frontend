{% extends "base.twig" %}

{% block content %}

<div class="blog">
  <ul class="blog__list">
    <li class="blog__item">
      <div class="blog__wrapper">
        <div class="blog__body">
          <div class="blog__image-wrapper">
              <?php the_post_thumbnail('medium', [
                'class' => 'blog__image',
                'title' => get_the_title(),
                'alt' => get_the_title()
                ]); ?>
          </div>

          <div class="blog__text">

          <h1 class="blog__title"><a href="{{post.link}}" title="{{post.title}}">{{post.title}}</a></h1>
            <div class="blog__meta">{{post.date}}</div>
            {{post.content}}
          </div>
        </div>
      </div>
    </li>
  </ul>
</div>

{% endblock %}