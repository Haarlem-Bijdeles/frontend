#import "./Fragments/PageContent.gql"

query GET_PAGE_BY_URI($uri: String) {
  page: pageBy(uri: $uri) {
    ...pageContent
    forStudentsAndParents {
      forParents {
        text
      }
      forStudents {
        text
      }
    }
    prices {
      price
      title
    }
  }
}
